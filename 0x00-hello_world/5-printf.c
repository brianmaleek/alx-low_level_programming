#include <stdio.h>
/**
 * main - this is the main function
 *
 * Return: always 0
 */
int main(void)
{
	char str1[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", str1);

	return (0);
}
